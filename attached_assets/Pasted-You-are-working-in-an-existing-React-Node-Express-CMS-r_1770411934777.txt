You are working in an existing React + Node/Express + CMS-rendered marketing site for BrushWhackers. The site currently feels flat/stale.

CRITICAL RULES:
- NON-DESTRUCTIVE changes only.
- Do not remove existing layouts or break existing pages.
- Implement changes via new reusable components, CSS variables, and small safe refactors.
- Ensure CMS block rendering benefits from the improvements automatically.

GOAL:
Increase design quality with a more “human” feel:
- Add depth (soft shadows, floating imagery, layered sections)
- Improve typography (better hierarchy, line length, readable rhythm)
- Add subtle motion (micro-interactions, hover/scroll reveals optional)
- Add texture (very subtle gradients + background patterns)
- Improve CTA components and section headers
- Ensure theme tokens still work; default theme remains blue

------------------------------------------------------------
PHASE 1 — DESIGN TOKENS (CSS VARIABLES) + BASE TYPOGRAPHY
------------------------------------------------------------

1) Create/extend a single global tokens layer (use existing system if present):
- Add CSS variables for:
  --surface, --surface-2, --border, --text, --muted
  --shadow-sm, --shadow-md, --shadow-lg
  --radius-sm, --radius-md, --radius-lg
  --grad-primary (subtle)
  --ring (focus)
  --container-max (e.g., 1100px)

2) Typography improvements:
- Use Inter everywhere (already desired)
- Set:
  - base font-size 16–18px
  - line-height 1.55–1.7
  - headings letter-spacing slightly tighter
  - limit paragraph width (e.g., max-w-prose)
- Improve spacing scale:
  - section padding: 64–96px desktop, 40–64px mobile
  - consistent gaps between headings/subtext/buttons

3) Add better link styles and focus rings:
- Focus-visible ring with blue tinted outline
- Remove harsh default outlines but keep accessibility

------------------------------------------------------------
PHASE 2 — NEW “PREMIUM” REUSABLE UI COMPONENTS
------------------------------------------------------------

Add new components (do not break old ones):
- Section
  - supports variants: "default", "tinted", "gradient", "pattern"
  - adds top/bottom padding and optional background overlay
- SectionHeaderPro
  - eyebrow text (small upper label)
  - H2 title
  - subtitle paragraph
  - optional right-aligned CTA
- CardPro
  - soft border + subtle shadow
  - hover lift (translateY -2px) + shadow increase
- ButtonPro
  - variants: solid, soft, outline
  - subtle hover + active press
- ImageFloat
  - wraps <img> with:
    - soft drop shadow
    - slight rotation option (-1deg to 1deg)
    - hover parallax micro-move
    - rounded corners
- Badge/Tag component for “Service Areas”, “Fast Quote”, etc.

Ensure these components consume CSS variables so theme presets still work.

------------------------------------------------------------
PHASE 3 — BACKGROUND TEXTURE + DEPTH (SUBTLE)
------------------------------------------------------------

1) Add a subtle background pattern option:
- A very faint noise or dot grid using CSS (no heavy images)
- Example: radial-gradient dots at 8–12% opacity

2) Add “section separators”:
- gentle gradient divider line
- optional angled/curved separator using SVG (very subtle; keep brand clean)

3) Add “spotlight” backgrounds behind hero CTAs:
- soft blue radial glow behind primary CTA area (low opacity)

------------------------------------------------------------
PHASE 4 — UPGRADE KEY PUBLIC SECTIONS (HOME + SERVICES + PRICING + BLOG)
------------------------------------------------------------

Without rewriting content, upgrade layout density and visuals:

HOME:
- Hero:
  - 2-column: left copy + right ImageFloat (equipment photo)
  - Add trust row beneath hero: “Local • Insured • Fast Quotes • Pro Equipment”
- “What We Clear”:
  - CardPro grid with icons and subtle hover
- “How It Works”:
  - Step cards with numbers in badges
- Testimonials:
  - Carousel optional; otherwise CardPro with stronger typography
- Final CTA:
  - gradient/tinted band with large button

SERVICES OVERVIEW:
- Service cards: ImageFloat thumbnail + CardPro
- Add “Popular in Charlotte area” tag row

SERVICE DETAIL PAGES:
- Add a sticky “Get a Quote” sidebar card on desktop
- Add an image gallery strip (2–3 images) using ImageFloat

PRICING:
- Make pricing cards more premium:
  - highlight “Best Value” plan with tinted background + badge
  - show effective hourly rate in a bigger style
  - include “What affects price” in a tinted section

BLOG:
- Blog list cards:
  - image thumbnail (ImageFloat small)
  - meta row (category/tag/date)
  - better spacing and hover state

------------------------------------------------------------
PHASE 5 — CMS BLOCK RENDERER VISUAL UPGRADE
------------------------------------------------------------

Update CMS block components to use the new premium components:
- hero → Section + SectionHeaderPro + ImageFloat
- rich_text → max-w-prose + better heading styles
- feature_grid/service_cards/testimonials/pricing_table/faq/cta_band → CardPro + Section variants
- Ensure block spacing is consistent across pages
- Add optional props support:
  - sectionVariant: default/tinted/gradient/pattern
  - emphasis: none/soft/highlight

Important:
- Keep backward compatibility: if blocks don’t have new props, use defaults.

------------------------------------------------------------
PHASE 6 — MICRO-INTERACTIONS (LIGHT, NOT GIMMICKY)
------------------------------------------------------------

Add subtle animations:
- Hover lift for cards/buttons
- ImageFloat hover drift (tiny)
- Optional scroll reveal (only if already using a library; otherwise keep simple CSS transitions)

Must respect prefers-reduced-motion.

------------------------------------------------------------
PHASE 7 — DOCS UPDATE (MANDATORY, CATEGORIZED)
------------------------------------------------------------

Update Admin → Docs using categories and the standard template.

Category: Theming
- “Design Tokens & CSS Variables”
- “Theme-Compatible Shadows, Radius, Gradients”

Category: CMS
- “Block Renderer Visual System (Premium Components)”
- “New Block Props (Section Variants / Emphasis)”

Category: Architecture
- “UI Component System v2 (Section/Card/Button/ImageFloat)”

Category: Performance
- “Visual Enhancements Performance Notes” (ensure no heavy assets; CSS-based textures)

Include:
- Which components were added
- How tokens map to theme presets
- Accessibility notes (focus rings, reduced motion)
- Guidance for future block styling

FINAL CHECKS:
- Site looks more dimensional (shadows, spacing, typography)
- No layout breaks on mobile
- CMS pages benefit automatically
- Prefers-reduced-motion respected
- Docs updated and easy to browse
